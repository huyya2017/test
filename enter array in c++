#include <iostream>
using namespace std;
void enterarray(int &n, int a[]);

int main()
{
  int n = 5;
  int a[n];
  enterarray(n,a);
  return 0;
}

void enterarray(int &n, int a[])
{
  for (int i = 0;i<n;i++)
  {
    cin >> a[i];
  }
}
